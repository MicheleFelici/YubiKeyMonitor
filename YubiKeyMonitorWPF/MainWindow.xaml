<Window x:Class="YubiKeyMonitorWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YubiKeyMonitorWPF"
        mc:Ignorable="d"
       WindowStyle="None"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent"
        Opacity="0.8"
        SizeToContent="WidthAndHeight"
        MouseEnter="Window_MouseEnter"
        MouseLeave="Window_MouseLeave">
    <Window.Resources>
        <Storyboard x:Key="FadeOutAnimation">
            <DoubleAnimation Duration="0:0:0.3"
                            Storyboard.TargetProperty="Opacity"
                            To="0.1"/>
        </Storyboard>
        <Storyboard x:Key="FadeInAnimation">
            <DoubleAnimation Duration="0:0:0.3"
                            Storyboard.TargetProperty="Opacity"
                            To="0.8"/>
        </Storyboard>
    </Window.Resources>
    <Grid>
        <!-- Aggiunto binding per la visibilità -->
        <Viewbox Width="100" Height="100" Visibility="{Binding ElementName=MainWindow, Path=Visibility}">
            <Border x:Name="OverlayBorder" CornerRadius="10" Padding="10">
                <Image x:Name="OverlayImage" 
           Source="./Resources/yubikey.png"
           MouseDown="OverlayImage_MouseDown"
           HorizontalAlignment="Center" 
           VerticalAlignment="Center" Opacity="0.8"/>
            </Border>
        </Viewbox>

        <tb:TaskbarIcon x:Name="TrayIcon"
                    IconSource="./Resources/yubikeyIcon.ico"
                    ToolTipText="YubiKey Monitor">
        <tb:TaskbarIcon.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Esci" Click="MenuExit_Click"/>
            </ContextMenu>
        </tb:TaskbarIcon.ContextMenu>
    </tb:TaskbarIcon>
    </Grid>
</Window>
